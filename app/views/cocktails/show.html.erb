<h1> <%= @cocktail.name %> </h1>
<!-- <h2><%= link_to '«- Back', cocktails_path %></h2> -->
<h2>
  <%= link_to new_cocktail_dose_path(@cocktail) do %>
    <i class="far fa-plus-square"> Add dose</i><br>
  <% end %>
</h2>

<ul>
  <% @cocktail.doses.each do |dose| %>
  <li>
    <%= dose.description %> <%= dose.ingredient.name %> <%= link_to 'x Remove dose', dose_path(dose), method: 'delete' %>
  </li>
  <% end %>
</ul>
